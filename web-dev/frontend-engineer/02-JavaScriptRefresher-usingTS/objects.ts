#!/usr/bin/env ts-node

/* class Person {
  constructor(public name: string, public age: number) {
    this.name = name;
    this.age = age;
  }
}

const p = new Person("Bob", 20);
console.log(p); */
