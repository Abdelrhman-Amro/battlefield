#!/usr/bin/env python3

x = 3.5
y = -10
z = 3

res = 0

# res = round(x) 
    # round(x) rounds x to the nearest integer
# res = abs(y)    
# res = pow(z, 2)
# res = max(x, y, z)
# res = min(x, y, z)

print(res)
